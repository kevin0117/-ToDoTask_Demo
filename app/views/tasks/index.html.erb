<h1><%=t("tasks.index_title")%></h1>

<table class="table">
  <tr>
    <td><%= t("tasks.task_id") %></td>
    <td><%= t("tasks.task_title") %></td>
    <td><%= t("tasks.task_content") %></td>
    <td><%= t("tasks.task_priority") %></td>
    <td><%= t("tasks.task_status") %></td>
    <td><%= t("tasks.task_begin") %></td>
    <td><%= t("tasks.task_end") %></td>
  </tr>

  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.id %></td>
      <%# <td>= link_to t('task_title', title: task.title), task_path(task) </td> %>
      <td><%= link_to task.title, task_path(task)  %></td>
      <td><%= task.content %></td>
      <td><%= t("tasks.priority_#{task.priority}") %></td>
      <td><%= t("tasks.status_#{task.status}") %></td>
      <td><%= task.task_begin %></td>
      <td><%= task.task_end %></td>
      <td><%= button_to t("tasks.edit"), edit_task_path(task), method: 'get' %></td>
      <td><%= button_to t("tasks.delete"), task_path(task), method: 'delete', data: { confirm: "確認刪除嗎？"} %></td>
    </tr>
  <% end %>
</table>

<%= button_to t("tasks.create"), new_task_path ,method: "get"%>

